[package]
name = "forge-world"
description = "Forge world frames: f64 reference frames in an integer sector grid, positions in cells for the GPU, cube-sphere and flat partitions with u64 cell ids, cell streaming plans."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
forge-core = { workspace = true }
glam = { workspace = true }
